// <auto-generated>
// From run-set.schema.json, by tools/generate-models.py 1.0.0.
// Schema sha256: 9042ee87dd9d0453f054db8f99281223b0ca3ad143c18201ed258be6a6cc79ad
//
// DO NOT EDIT. Run the generator; a hand-edited file stops being generated
// and starts being a second, quieter contract.
//
// THIS TYPE CARRIES THE SHAPE AND NOT THE RULES. The schema uses constructs
// that no C# type expresses, so deserialising successfully is not the same as
// honouring the contract:
//   const
//   minimum
//
// VALIDATE AGAINST THE SCHEMA AS WELL. Two settings are required and are not
// the defaults:
//
//   UnmappedMemberHandling = Disallow          unknown members are ignored
//                                              by default; these schemas
//                                              refuse them
//   RespectRequiredConstructorParameters = true a missing required member is
//                                              filled with null by default
//
// Both were verified against a compiler rather than assumed, and even with
// both the model is narrower than the contract. Demonstrated: a property that
// is optional and NOT nullable, such as an optional `string`, permits absence
// and refuses null. C# collapses the two into the same `null`, so
//
//   {"tenant": null}   the model accepts it, the schema refuses it
//
// Deserialising successfully is therefore not honouring the contract, and the
// order that holds is validate, then deserialise.
// </auto-generated>
#nullable enable
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Ph7x.Governance.Contracts;

public sealed record RunSet(
    /// <summary>How many resources were observed. It cannot say how many the identity failed to return, and `run_coverage` is where that limit is recorded rather than inferred.</summary>
    [property: JsonPropertyName("resources")] int Resources,
    /// <summary>Resource class to how many carried it. A label, never a filter.</summary>
    [property: JsonPropertyName("by_class")] IReadOnlyDictionary<string, int> ByClass,
    /// <summary>How many runs a profile moved BELOW THE FOLD. Not how many were skipped: their findings are evaluated, counted and printed, just lower down. This said 'set aside rather than evaluated', which is the opposite of what the engine does, and a consumer implementing the contract as written would report evaluated resources as unevaluated.</summary>
    [property: JsonPropertyName("set_aside")] int SetAside,
    [property: JsonPropertyName("counts")] Counts Counts,
    /// <summary>The same idea a Run calls `coverage`, under a different name. `expected` is deliberately optional here: counting documents proves how many resources were observed and never how many were missed, so its absence is stored as not-established rather than as complete coverage.</summary>
    [property: JsonPropertyName("run_coverage")] JsonElement RunCoverage,
    /// <summary>Each run whole. Duplicate resource ids are refused at construction, because two documents describing one resource is a collection defect and not a set of two.</summary>
    [property: JsonPropertyName("runs")] IReadOnlyList<Run> Runs,
    /// <summary>The exact contract this document claims, which must be the $id of the schema validating it. An instance convention and not the dialect keyword: inside a schema document `$schema` names the JSON Schema dialect, and using one field for both meanings is what this separation exists to prevent. It replaces `schema_version`, which was a second version that could not even express the one in the $id — `1.0` against a contract ending in `/1.2.0` — and was maintained by hand.</summary>
    [property: JsonPropertyName("$schema")] string Schema,
    /// <summary>WHAT WAS OBSERVED, BY SERVICE AND BY AREA, so a consumer opens a result and sees which parts of Microsoft 365 were looked at before it sees a rule identifier. A reader was shown findings grouped by whatever the interface chose, which describes this engine's internals rather than their tenant. PUBLISHED RATHER THAN LEFT TO BE COMPUTED. A consumer deriving this would be a second authority on what a service is and what an area is, and the first thing it would disagree about is a service that collected evidence and reached no conclusion. NO PERCENTAGE IS DERIVABLE FROM THIS AND NONE IS INTENDED. `conclusions` is a count of conclusions, never a numerator: the denominator would be the number of questions somebody could have asked, which nobody knows, and `D5` forbids the aggregate that would produce.</summary>
    [property: JsonPropertyName("services")] IReadOnlyList<RunSetServicesItem> Services
);
public sealed record RunSetServicesItem(
    /// <summary>The workload the evidence names, never a label chosen here.</summary>
    [property: JsonPropertyName("service")] string Service,
    /// <summary>THE WEAKEST AREA THIS SERVICE HAS. Reporting the strongest would be the aggregate this product refuses, in the place a reader looks first: a service that read four things and could not read one is not observed.</summary>
    [property: JsonPropertyName("state")] string State,
    /// <summary>Results that decided something, counted in the area they were decided FROM. A rule reading two areas counts in both, because picking one would be tidiness asserted as fact.</summary>
    [property: JsonPropertyName("conclusions")] int Conclusions,
    /// <summary>Results that are `unknown` or `invalid-evidence`. Kept apart from conclusions because a rule that could not decide is not a rule that decided nothing is wrong.</summary>
    [property: JsonPropertyName("unresolved")] int Unresolved,
    /// <summary>What the collector declared it requested, and how far each reading got.</summary>
    [property: JsonPropertyName("areas")] IReadOnlyList<RunSetServicesItemAreasItem> Areas
);
public sealed record RunSetServicesItemAreasItem(
    [property: JsonPropertyName("area")] string Area,
    /// <summary>`not-observed` is a reading nobody made; `refused` is one the tenant or this product declined; `partial` is one that returned some of what it covers. Three states that look alike on a screen and are not the same, and a product collapsing them describes a collector that never ran exactly as one that ran and could not answer.</summary>
    [property: JsonPropertyName("state")] string State,
    [property: JsonPropertyName("conclusions")] int Conclusions,
    [property: JsonPropertyName("unresolved")] int Unresolved,
    /// <summary>How many documents contributed to this area. A count, never the things counted.</summary>
    [property: JsonPropertyName("population")] int Population,
    /// <summary>The collectors whose documents fed this area, from their own provenance.</summary>
    [property: JsonPropertyName("acquisition")] IReadOnlyList<string> Acquisition,
    /// <summary>WHY, IN THE COLLECTOR'S WORDS, where it gave a reason. A state without one tells a reader that something is partial and not WHAT part, which is the shape of a status nobody can act on. Null where the reading succeeded and there was nothing to explain.</summary>
    [property: JsonPropertyName("detail")] string? Detail,
    /// <summary>When this area was last read. A reading is true of a moment, and an artefact opened a year later describes the tenant of that day. The LATEST of the documents that fed it, because what a reader wants is how current this is rather than when it first happened.</summary>
    [property: JsonPropertyName("observed_at")] string? ObservedAt
);
