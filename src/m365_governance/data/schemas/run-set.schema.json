{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ph7x.com/schemas/m365-governance/run-set/1.0.0",
  "title": "RunSet",
  "description": "An evaluation over more than one resource, exactly as `RunSet.to_dict()` emits it. A directory is a stable input shape whether it holds one document today and fifty tomorrow, so its result is an envelope rather than a bare list. Described as it is: two field names disagree with the Run envelope and that is recorded here rather than corrected.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "run_schema_version",
    "resources",
    "by_class",
    "set_aside",
    "counts",
    "run_coverage",
    "runs"
  ],
  "properties": {
    "run_schema_version": {
      "type": "string",
      "description": "The version of this envelope. Note that a Run has no equivalent field, so the set is versioned and the individual run is not."
    },
    "resources": {
      "type": "integer",
      "minimum": 0,
      "description": "How many resources were observed. It cannot say how many the identity failed to return, and `run_coverage` is where that limit is recorded rather than inferred."
    },
    "by_class": {
      "type": "object",
      "description": "Resource class to how many carried it. A label, never a filter.",
      "additionalProperties": {
        "type": "integer",
        "minimum": 0
      }
    },
    "set_aside": {
      "type": "integer",
      "minimum": 0,
      "description": "How many runs were set aside rather than evaluated."
    },
    "counts": {
      "$ref": "https://ph7x.com/schemas/m365-governance/run/1.0.0#/$defs/counts"
    },
    "run_coverage": {
      "type": "object",
      "description": "The same idea a Run calls `coverage`, under a different name. `expected` is deliberately optional here: counting documents proves how many resources were observed and never how many were missed, so its absence is stored as not-established rather than as complete coverage."
    },
    "runs": {
      "type": "array",
      "description": "Each run whole. Duplicate resource ids are refused at construction, because two documents describing one resource is a collection defect and not a set of two.",
      "items": {
        "$ref": "https://ph7x.com/schemas/m365-governance/run/1.0.0"
      }
    }
  }
}
