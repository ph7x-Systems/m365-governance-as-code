{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ph7x.com/schemas/m365-governance/comparison/1.0.0",
  "title": "Comparison",
  "description": "The relation between two assessments, and the only place a diff belongs. A diff is not part of an assessment: it is a claim about two states, and putting it inside one would make that state carry an assertion about another. This may be cached, exported or shipped inside a delivery package, and it stays derivable in all three cases: given both assessments, the engine reproduces it.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "before",
    "after",
    "diff"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "before": {
      "$ref": "#/$defs/side"
    },
    "after": {
      "$ref": "#/$defs/side"
    },
    "diff": {
      "$ref": "#/$defs/diff"
    }
  },
  "$defs": {
    "side": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "assessment_id",
        "canonical_hash",
        "created_at"
      ],
      "description": "Which assessment, by identity rather than by copy. A comparison that embedded both would duplicate the canonical truth it is describing.",
      "properties": {
        "assessment_id": {
          "type": "string",
          "minLength": 1
        },
        "created_at": {
          "type": "string",
          "format": "date-time"
        },
        "tenant": {
          "type": "string"
        },
        "canonical_hash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "The assessment's canonical digest. Identity that can be verified rather than trusted, and the reason a comparison never carries a path: a path says where a file sat on one machine, not what it held."
        }
      }
    },
    "diff": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "produced_by",
        "changes"
      ],
      "properties": {
        "produced_by": {
          "type": "string",
          "minLength": 1,
          "description": "The engine version that computed it. A diff nobody can reproduce is an opinion about history."
        },
        "changes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/change"
          }
        }
      }
    },
    "change": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "resource",
        "rule",
        "before",
        "after",
        "kind",
        "causes"
      ],
      "properties": {
        "resource": {
          "type": "string",
          "minLength": 1
        },
        "rule": {
          "type": "string",
          "minLength": 1
        },
        "before": {
          "$ref": "#/$defs/outcome"
        },
        "after": {
          "$ref": "#/$defs/outcome"
        },
        "kind": {
          "type": "string",
          "enum": [
            "added",
            "removed",
            "changed"
          ],
          "description": "What happened to this resource-and-rule pair between the two sides. Deliberately three values: what changed is a different question from why, and answering both in one field forces a simultaneous change to hide one of its halves."
        },
        "causes": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "description": "Why the two sides differ, and more than one may be true at once. A run where the tenant moved AND the rule was reissued has both causes, and a model that made them exclusive would report a rule change as tenant drift or the reverse.",
          "items": {
            "type": "string",
            "enum": [
              "evidence-changed",
              "outcome-changed",
              "rule-version-changed"
            ]
          }
        }
      }
    },
    "outcome": {
      "description": "Five states, never three, and `null` where the side did not carry this pair at all.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "pass",
            "fail",
            "unknown",
            "not-applicable",
            "invalid-evidence"
          ]
        },
        {
          "type": "null"
        }
      ]
    }
  }
}
